# Voice anchors

Reference clips for zero-shot cloning, passed as the reference audio on a narration
request. They define voice identity, which no generation parameter can change.

The locked set is in `locked/` — see its README. Nothing else belongs here.
