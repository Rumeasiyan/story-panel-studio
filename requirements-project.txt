# Project-level utilities only. Anything ComfyUI itself controls lives in
# engine/ComfyUI/requirements.txt and must not be duplicated or re-pinned here.

huggingface_hub
PyYAML
requests
rich

# Local web UI (service/)
fastapi
uvicorn[standard]
python-multipart

# Subtitle alignment (service/pipelines/subtitles.py). The `subtitles` pipeline is
# registered unconditionally, so without this a fresh bootstrap yields a pipeline that
# accepts jobs and then fails with ModuleNotFoundError.
faster-whisper
soundfile
